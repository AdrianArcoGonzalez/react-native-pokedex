import {StyleSheet} from 'react-native';

const headerStyle = StyleSheet.create({
    headerContainer: {
        marginBottom: 20,
        marginLeft: 20,
    },
    title: {
        color: 'black',
        fontSize: 25,
        fontWeight: 'bold',
    },
});

export default headerStyle;
